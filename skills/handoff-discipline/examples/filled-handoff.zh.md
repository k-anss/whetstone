# 示例 · 一份真实交接(脱敏版)

这就是产出本 skill 的那份交接:一个决策会话把施工交给一个零共享
上下文的执行会话。人名约简为"K";其余按实际使用呈现。

---

# 交接 · whetstone skill 施工 → 全新执行会话

## 项目一句话
一个 skills monorepo(仓名 `whetstone`,账号 `k-anss`,MIT,中英双轨),
每个 skill 把一项领域判断编码成 agent 可执行的纪律文件。

## 已拍死决定(不许重开)
1. skill 命名:`handoff-discipline`。备选项已在决策端考虑并否决。
2. 仓库:`whetstone`,monorepo,skill 挂 `skills/` 下。账号:`k-anss`。
   许可:MIT。署名:K。
3. 结构:SKILL.md + 3 个 reference(layers / template / receiving)+
   1 个 example,每个都带 `.zh` 孪生层。
4. SKILL.md 的 description 文本:决策端已定稿,含负触发,原样照用。
5. 一个会话只建一个 skill,单遍成型。返工诉求记录后带回决策端——
   不在本会话修。

## 当前状态
- 生产简报已在决策端写完、批准。
- 尚未产出任何文件;执行会话从零开始。

## 待定项(非已拍死——等拍板)
- 英文 README 立场句最终措辞 — 待定:K 定稿。
- 是否需要第四个 reference — 待定:首次外部使用后再看。

## 战略理由(供理解,不供修改)
本 skill 的差异化在判断密度,不在功能数量。所有增厚材料(机制 /
观察 / 边界)必须出自作者真实实践;禁止上网搜"业界最佳实践"做
填充,因为那会抹掉出处。

## 红线
- 不出现真实单位名、市县级具体地名、真实人名、可反查工作细节。
  示例数据虚构或脱敏。
- 不用 emoji,不用引流话术。

## 接手方:角色与回执
你按已拍死结构施工。不改名、不重组结构、不重审 description 文本。
开工前回执:原样回显已拍死清单,列出你读到的待定项,说出第一个
具体动作。

## 冲突优先级
用户当场说法 > 本文档 > 任何更早文档。

---

这个示例凭什么入选:上面这套决策端 / 执行端分工,正是本 skill 提炼
自的那套实践。这份交接不是为演示而摆拍——你正在读的这个 skill,
就是经它建出来的。
