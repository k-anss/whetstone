# 改编测试痕迹页 — 对 humanizer-zh

本 skill 是改编线产物,derived from / inspired by humanizer-zh,方向相反:**删,不是加**。本页是用真实文本证明这一差异的测试记录。

**状态:模板 — 待 K 首次真实测试回填。** 初次构建时执行端无法实际运行 humanizer-zh。下方无任何编造结果;编造测试记录既违反构建母法,也与本 skill 的主题自相矛盾。

## 测试流程(首次运行时填)

1. **输入**:一段按删词测试纪律写成的克制留档中文。脱敏后贴在此处。
2. **A 轮 — humanizer-zh**:对输入运行,逐字记录输出。标出它**加上**的每个元素(助词、温度词、口语、emoji)。
3. **B 轮 — strip-the-ai-voice**:对 A 轮输出运行本 skill,记录输出。确认每处改动都是**删除**,且能对应到四类删除或禁区清单之一——没有改写。
4. **结论**:两行写清 A 轮加了什么、B 轮删了什么。这个差值就是两个工具方向相反的实证。

## 待填

```
输入(脱敏):
[待回填]

A 轮输出(humanizer-zh):
[待回填]
被加上的元素:
[待回填]

B 轮输出(本 skill):
[待回填]
删除项(按类):
[待回填]

结论:
[待回填]
```

— 由 K 首次真实测试后回填。在此之前,谱系主张只依据 README 里陈述的设计层差异(删 vs 加),不依据实证结果。
